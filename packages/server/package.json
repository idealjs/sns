{
  "name": "@idealjs/mono-template-server",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "generate:dev": "openssl req -x509 -newkey rsa:2048 -nodes -sha256 -subj '/CN=localhost' -addext 'subjectAltName = DNS.1:localhost' -keyout server.key -out server.crt",
    "dev": "nodemon"
  },
  "devDependencies": {
    "@types/koa__router": "^12.0.0",
    "@types/minio": "^7.0.15",
    "@types/node": "^18.11.14",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@idealjs/mono-template": "^1.0.0",
    "@koa/router": "^12.0.0",
    "koa": "^2.14.1"
  }
}
